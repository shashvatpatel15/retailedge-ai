# RetailEdge AI - Frontend Environment Template
# If running backend locally on this PC:
VITE_API_URL=http://127.0.0.1:8000/api/v1

# If connecting to Raspberry Pi on the local network (replace with your Pi's IP):
# VITE_API_URL=http://<YOUR_PI_IP>:8000/api/v1

# Optional Edge API Key (if EDGE_API_KEY is enabled on backend):
# VITE_EDGE_API_KEY=your_secret_key_here
